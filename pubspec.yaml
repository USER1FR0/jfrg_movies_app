name: jfrg_movies_app
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter
  animate_do: ^3.1.2
  card_swiper: ^3.0.1
  dio: ^5.4.0
  flutter_dotenv: ^5.1.0
  flutter_riverpod: ^2.4.10
  go_router: ^13.2.0
  youtube_player_flutter: ^8.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

dependency_overrides:
  flutter_inappwebview: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - .env

